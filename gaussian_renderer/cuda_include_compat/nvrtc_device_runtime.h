#pragma once
// CUDA's nv/target header names this file only in the NVRTC branch.  The
// production binding is compiled by nvcc; this guarded compatibility header
// exists so Jittor's unconditional quoted-include scanner can resolve it.
